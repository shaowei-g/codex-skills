interface:
  display_name: 'Spec Drift Check'
  short_description: 'Detect spec scope drift and alignment gaps'
  default_prompt: 'Use $spec-drift-check to decide whether the latest changes exceed the current spec.'
