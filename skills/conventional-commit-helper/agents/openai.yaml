interface:
  display_name: "Conventional Commit Helper"
  short_description: "Write Conventional Commits with body and footer"
  default_prompt: "Use $conventional-commit-helper to draft a Conventional Commit message with a type(scope): subject header, optional body, and optional footer."
